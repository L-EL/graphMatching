# graphMatching

how to use : 

1) fill the file names in the code

2) change the matching labels (seed for the matching)

3) choose if you want to "realigned" the organs (do vecOrgan1 = extractOrganOrientation(imgSeg1, np.median(imgSeg1)), like done in the 3D version)  or use the image orientation (do vecOrgan1 = [0,1] , like done in the 2D version) by changing in the code
